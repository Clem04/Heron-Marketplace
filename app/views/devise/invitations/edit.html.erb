<h2><%= t "devise.invitations.edit.header" %></h2>

<h6>Complete this form as it will be the information displayed to the Heron Marketplace customers. As a company which pride itself in offering products to our customers from brands and female founded companies who carry their business activities integrity, we want to be as transparent as possible ourselves. the following information will be displayed on your personal merchant profile page.</h6>

<%= simple_form_for(resource, as: resource_name, url: invitation_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>
  <%= f.hidden_field :invitation_token %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :public_email %>
    <%= f.input :location %>
    <%= f.input :description, :as => :text %>
    <%= f.input :website %>
    <%= f.input :instagram %>
    <%= f.input :facebook %>
    <%= f.input :picture_1, as: :file %>
    <%= f.input :picture_2, as: :file %>
    <%= f.input :picture_3, as: :file %>
    <%= f.input :picture_4, as: :file %>
    <%= f.input :password %>
    <%= f.input :password_confirmation %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, t("devise.invitations.edit.submit_button") %>
  </div>
<% end %>
