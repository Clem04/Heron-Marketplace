<ul>
  <% @cart.line_items.each do |line_item| %>
    <li><%= line_item.product.name %> -
        <%= link_to line_item_path(line_item.id), method: "delete", data: { confirm: "Are you sure?" } do %>
        <i class="far fa-trash-alt"></i>
      <% end %>
    </li>
  <% end %>
</ul>
